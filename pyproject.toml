[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ebu6307-stereo-bootstrap"
version = "0.1.0"
description = "Flat codes/ layout for the EBU6307 stereo depth mini project"
requires-python = ">=3.10"
